# solana-program-release-skill
Solana AI Kit skill for release-readiness checks on Solana/Anchor programs. It reviews Anchor.toml, declare_id!, IDL artifacts, tests, CI and deploy evidence, then generates Markdown/JSON reports. Read-only, no network calls, no keypair access, with clear human checks for upgrade authority and governance.
